{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://decentraid-server.azurewebsites.net/api/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/*.{js,css,ico,png,svg}"]
  }
}
